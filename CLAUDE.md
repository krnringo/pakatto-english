# CLAUDE.md

このファイルは毎セッション読み込まれる。書いてよいのは「これがないとClaudeが間違える」ことだけ。
迷ったら削る側に倒す(長すぎるCLAUDE.mdは、重要なルールほど埋もれて無視される)。

## 開発フロー(必須)

新機能・仕様変更のある作業は次の順で進める:

**要件定義 → 実装 → レビュー → コミット**

1. 曖昧な依頼、複数ファイルにまたがる変更、仕様が固まっていない機能追加は、まず `requirements` スキルで
   `docs/specs/{NN-feature}.md` を作ってから着手する。
2. 実装は `implement` スキルの手順(検証可能な単位に分解→都度実行して確認)に従う。
   タスクが複雑・曖昧・被害の大きい変更(本番設定・データ移行・外部送信)を含むなら `deep-task` スキル
   (グローバル: `~/.claude/skills/deep-task/`)も読む。
3. 完了したと言う前に `review` スキル(OpenAI Codex `codex exec` による外部レビュー→Claude Codeがトリアージ)を通す。
4. コミットメッセージは "why" を書く。"what" は diff が語る。

**例外**: タイポ修正・1行のログ追加・変数名変更など、diffを一文で言い切れる小さな変更はこの手順を省略してよい。
計画のオーバーヘッドは常に有用とは限らない。

## 検証の鉄則

「動くはず」は禁句。実行して確認したことだけを「完了」と報告する。
テスト/ビルド/lintの実行コマンド: `(ここにこのプロジェクト実際のコマンドを書く。例: npm test / npm run typecheck)`

## コードスタイル

`(このプロジェクト固有の規約だけ書く。言語のデフォルト慣習は書かない=Claudeは既に知っている)`

## Gitワークフロー

- 明示的な指示がない限り `--amend` `push --force` `reset --hard` は使わない
- コミット前に `git status` で意図しないファイルが混ざっていないか確認する

## 詳細ドキュメント

- 開発の進め方(全体像): @docs/development-guide.md
- 要件定義済みの仕様一覧: @docs/specs/

---
> このテンプレートの使い方: 新しいアプリ開発を始めるとき、このリポジトリの中身(CLAUDE.md / .claude/ / docs/)を
> 新しいプロジェクトフォルダにそのままコピーし、上記の `(...)` 部分をプロジェクトに合わせて埋める。
